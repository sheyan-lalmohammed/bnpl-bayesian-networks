# ---------------------------------------------------------------------------
# Large data files — NOT stored in git (multiple GB, exceed GitHub's limits).
# Regenerate them by running the stage-00 and stage-01 notebooks.
# See data/README.md for details.
# ---------------------------------------------------------------------------

# SQLite databases
*.db
*.sqlite
*.sqlite3

# Raw Lending Club exports
accepted_*.csv
rejected_*.csv
cleaned_accepted_loans.csv

# Rebalanced / discretized / dynamic intermediate datasets
rebalanced_age_data*.csv
bnpl_loans_with_age_change.csv

# But DO keep the small committed result & reference files (whitelist):
!**/bootstrap_results*.csv
!**/reference_outputs/**
!**/example_cpd_tables/**

# ---------------------------------------------------------------------------
# Python / Jupyter
# ---------------------------------------------------------------------------
__pycache__/
*.py[cod]
.venv/
venv/
env/
.ipynb_checkpoints/
.python-version

# ---------------------------------------------------------------------------
# OS / editor
# ---------------------------------------------------------------------------
.DS_Store
Thumbs.db
.idea/
.vscode/
